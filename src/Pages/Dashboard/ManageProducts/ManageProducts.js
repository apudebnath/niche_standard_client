import { Container, Typography } from '@mui/material';
import React from 'react';

const ManageProducts = () => {
    return (
        <Container sx={{mb:'150px'}}>
            <Typography variant="h4" sx={{pt:8}}>Manage Products</Typography>
            <Typography variant="h5" sx={{py:8}}>Products managment system under procedure. Coming soon</Typography>
        </Container>
    );
};

export default ManageProducts;